<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.isaacsufyan.androidarchitecture.tdd.testapp">

    <application
        android:name="com.isaacsufyan.androidarchitecture.tdd.testapp.TodoApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.isaacsufyan.androidarchitecture.tdd.testapp.tasks.TasksActivity"
            android:exported="true"
            android:theme="@style/AppTheme.OverlapSystemBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
